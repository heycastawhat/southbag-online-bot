{
  "name": "southbag-slack-bot",
  "version": "1.0.0",
  "description": "Southbag Online Banking - Slack Bot (deliberately terrible customer support)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy",
    "convex:dev": "npx convex dev",
    "convex:deploy": "npx convex deploy"
  },
  "dependencies": {
    "convex": "^1.17.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
